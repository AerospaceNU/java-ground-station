plugins {
    id 'java'
}

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.apache.commons:commons-csv:1.10.0'
}

// Optional: set Java compatibility if needed
java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(11)
    }
}